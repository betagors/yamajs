{
  "name": "yama-example-todo-api",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "tsx dev-server.mjs",
    "start": "tsx dev-server.mjs",
    "typegen": "yama-typegen yama.yaml src/types.ts",
    "build": "echo \"Example doesn't need building, but ensure dependencies are built: pnpm build:packages\""
  },
  "dependencies": {
    "@yama/runtime-node": "workspace:*",
    "@yama/core": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.7.0"
  }
}

