# Yama generated files
.yama/

# Old generated files (can be removed after migration)
src/generated/
lib/generated/
src/types.ts

# Environment files (keep .env.example files, ignore actual .env files)
.env
.env.local
.env.*.local