# Official Plugins

Plugins maintained by the Yama team.

## Database

### @yamajs/postgres

PostgreSQL adapter with connection pooling and migrations.

```bash
pnpm add @yamajs/postgres
```

```yaml
plugins:
  - name: "@yamajs/postgres"
    config:
      url: ${DATABASE_URL}
      pool:
        min: 2
        max: 10
```

### @yamajs/pglite

Embedded PostgreSQL using PGLite. Great for development and testing.

```bash
pnpm add @yamajs/pglite
```

```yaml
plugins:
  - name: "@yamajs/pglite"
    config:
      dataDir: ./data
```

## Storage

### @yamajs/s3

AWS S3 compatible storage adapter.

```bash
pnpm add @yamajs/s3
```

```yaml
plugins:
  - name: "@yamajs/s3"
    config:
      bucket: ${S3_BUCKET}
      region: ${AWS_REGION}
      accessKeyId: ${AWS_ACCESS_KEY_ID}
      secretAccessKey: ${AWS_SECRET_ACCESS_KEY}
```

**Usage:**

```typescript
export async function uploadFile(context: HandlerContext) {
  const { s3 } = context.services;
  
  const result = await s3.upload({
    key: 'uploads/file.pdf',
    body: context.request.body,
    contentType: 'application/pdf',
  });
  
  return { url: result.url };
}
```

## Caching

### @yamajs/redis

Redis adapter for caching and session storage.

```bash
pnpm add @yamajs/redis
```

```yaml
plugins:
  - name: "@yamajs/redis"
    config:
      url: ${REDIS_URL}
```

**Usage:**

```typescript
export async function getData(context: HandlerContext) {
  const { redis } = context.services;
  
  // Check cache
  const cached = await redis.get('my-key');
  if (cached) return JSON.parse(cached);
  
  // Fetch and cache
  const data = await fetchExpensiveData();
  await redis.set('my-key', JSON.stringify(data), 'EX', 3600);
  
  return data;
}
```

## Realtime

### @yamajs/realtime

WebSocket support for real-time updates.

```bash
pnpm add @yamajs/realtime
```

```yaml
plugins:
  - name: "@yamajs/realtime"
    config:
      path: /ws
```

**Usage:**

```typescript
export async function createTodo(context: HandlerContext) {
  const { realtime } = context.services;
  
  const todo = await context.db.query(
    'INSERT INTO todos (title) VALUES ($1) RETURNING *',
    [context.request.body.title]
  );
  
  // Broadcast to connected clients
  await realtime.broadcast('todos', {
    type: 'created',
    data: todo[0],
  });
  
  return todo[0];
}
```

## All Packages

| Package | Description |
|---------|-------------|
| `@yamajs/cli` | Command-line interface |
| `@yamajs/core` | Core runtime and types |
| `@yamajs/fastify` | Fastify HTTP adapter |
| `@yamajs/postgres` | PostgreSQL adapter |
| `@yamajs/pglite` | PGLite adapter |
| `@yamajs/redis` | Redis adapter |
| `@yamajs/s3` | S3 storage adapter |
| `@yamajs/realtime` | WebSocket realtime |
| `@yamajs/sdk` | SDK generator |
| `@yamajs/openapi` | OpenAPI generator |
