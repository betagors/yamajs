# Yama Transporters

Transporters are the interface layer of Yama. They are responsible for receiving external requests (HTTP, GraphQL, RPC, etc.) and translating them into calls to the Yama Kernel operations.

## Available Transporters

- **HTTP**: Standard REST/HTTP interface.
- **GraphQL**: GraphQL interface (Future).
- **MCP**: Model Context Protocol interface (Future).
