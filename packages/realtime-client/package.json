{
  "name": "@betagors/yama-realtime-client",
  "version": "0.1.0-alpha.0",
  "type": "module",
  "description": "Yama realtime client SDK for WebSocket connections",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/betagors/yama.git",
    "directory": "packages/realtime-client"
  },
  "keywords": [
    "yama",
    "realtime",
    "websocket",
    "client",
    "sdk"
  ],
  "license": "MPL-2.0",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "dev": "tsc -w -p tsconfig.build.json",
    "lint": "echo \"no lint yet\"",
    "test": "echo \"no tests yet\"",
    "test:watch": "vitest --watch",
    "test:coverage": "vitest --coverage",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "rimraf": "^5.0.0",
    "@types/node": "^20.0.0",
    "vitest": "^1.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}

