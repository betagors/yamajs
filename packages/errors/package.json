{
  "name": "@betagors/yama-errors",
  "version": "0.1.0-alpha.0",
  "type": "module",
  "description": "Yama — standardized error handling and error types",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/betagors/yama.git",
    "directory": "packages/errors"
  },
  "keywords": [
    "yama",
    "errors",
    "error-handling",
    "api-errors",
    "typescript"
  ],
  "license": "MPL-2.0",
  "scripts": {
    "build": "pnpm exec tsc --build --force tsconfig.build.json",
    "dev": "pnpm exec tsc --build -w tsconfig.build.json",
    "lint": "eslint . --ext .ts,.tsx",
    "lint:fix": "eslint . --ext .ts,.tsx --fix",
    "test": "vitest",
    "test:watch": "vitest --watch",
    "test:coverage": "vitest --coverage",
    "clean": "rimraf dist tsconfig.build.tsbuildinfo"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@vitest/coverage-v8": "^1.0.0",
    "rimraf": "^5.0.0",
    "vitest": "^1.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
